apiVersion: v1
kind: Service
metadata:
  name: acss-mongo
spec:
  ports:
  - port: 27017
    name: acss-mongo
  selector:
    app: acss-mongo
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: acss-mongo-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi
---
apiVersion: apps/v1beta2 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: acss-mongo
spec:
  selector:
    matchLabels:
      app: acss-mongo
  strategy:
    type: Recreate      
  template:
    metadata:
      labels:
        app: acss-mongo
    spec:
      containers:
      - image: mongo:3.7-jessie
        name: acss-mongo        
        ports:
        - containerPort: 27017
          name: acss-mongo
        volumeMounts:
        - name: acss-mongo-persistent-storage
          mountPath: /data/db
      volumes:
      - name: acss-mongo-persistent-storage
        persistentVolumeClaim:
          claimName: acss-mongo-pv-claim
